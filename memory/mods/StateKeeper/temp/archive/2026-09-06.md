# StateKeeper 临时恢复记录（2026-09-06）

## 当前判断

- `StateKeeper 0.1.0` 已完成采集、分块、后台写盘、收藏、最近 10 局、RunId 恢复和正式结束判断；尚未正式发布，也没有分析面板。
- 改名前一局约 75 分钟数据证明 GZip 体积可接受，但事件严重膨胀：65,150 条体力事件占总事件约 74.3%，绝大多数小于 0.01。当前已用可配置阈值 0.01 过滤，5Hz 曲线不变。
- 旧数据已人工一次迁移到 `LocalLow/LandCrab/PEAK/StateKeeper`；旧路径运行时兼容代码已按用户要求删除，后续不要恢复。

## 证据

- 128 个分块全部正常解压，编号 0-127 连续，总压缩约 4.80 MiB、原始 JSON 约 88.69 MiB。
- 21,367 个采样覆盖约 4,460.84 秒，实际约 4.79Hz；一局累计见过 14 个玩家身份。
- profile 只保留 `StateKeeper.dll`；配置为 `com.local.statekeeper.cfg`；严格构建 0/0，测试 2/2。

## 风险与下一步

- 用当前优化 DLL 跑一局多人长时间测试，重新统计 `StaminaChanged` 数量、GZip 体积、WhySoLaggy 中 `StateKeeper`/`Character.UseStamina` 耗时及 GC。
- 调查旧数据 11 次时间倒退（最大约 1.353 秒）和短时 9-10 个角色的来源。
- 在真实数据足够前不做分析面板；当前 `发行/0.1.0` 是旧名草稿，不能发布。

## 后续方案（仅计划）

- 展示名改为 `STATE KEEPER` / “状态分析”，程序身份和 `0.1.0` 不变。
- 采集仍写碎块，正式结束后后台合并为单局整体 `*.data.json.gz`；合并和分析各自使用独立进度条。
- 面板参考 WhereIsThing 和 LengSword-ClimbInfoTracker，从 ESC 暂停菜单进入，收藏移入面板，后续删除 F8。
- PEAK-MAP 后续使用独立 StateKeeper API、Supabase 元数据和私有 Cloudflare R2；客户端/服务端双重脱敏，用户明确同意，网站可关闭。当前不改任何源码。
