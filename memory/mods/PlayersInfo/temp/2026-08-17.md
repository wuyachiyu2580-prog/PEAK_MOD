# 2026-08-17 PlayersInfo 临时记忆

## 当前结论

- 已修复额外体力物品“进度完成但无效果”：队友条只克隆 `StaminaBar` 子树时，克隆组件的 `afflictions` 可能仍引用子树外的本地原版 `BarAffliction`；旧代码随后禁用并销毁这些原版组件，导致 `Action_GiveExtraStamina -> AddExtraStamina -> GUIManager.bar.ChangeBar()` 中断。
- 队友异常条现在只转换 `teammate clone root` 内的组件；子树外的原版模板会先复制成队友私有对象，销毁前强制做 `IsChildOf` 所有权检查。
- 用户进一步用截图说明：红框下方是本机原版额外条，红框内是本机主体力条和队友额外图形条发生重叠；要隐藏的是后者，不是队友主体力条或右侧 `current/cap`。现在会立即禁用克隆的原版 `StaminaBar`，并只删除克隆根内部由 `extraBar`、`extraBarStamina`、`extraBarOutline`、`extraStaminaIcon`、`extraStaminaGlow` 引用或明确命名为 `Extra*` 的视觉对象；右侧 `current/cap` 保留。
- 后续清晰截图确认最后残留的紫色 `50` 条实际来自 `BarAffliction.isPetrify`。原版把它作为独立额外体力行的石化上限段；队友克隆后沿用本机 BarGroup 坐标，因而压到本机主体力条上。`BuildClonedAfflictions()` 现在不克隆队友石化图形，并清理克隆树内已有的 petrify 组件；普通队友异常条继续保留，石化上限通过 `current/cap` 的分母表达。
- `AddStretchText()` 不再为了添加 TMP 而强制启用原版额外体力子节点，原版显隐和动画继续由 PEAK 控制。
- 不整体回退到 `0.2.0`。这样会让 `0.2.1` 已成熟的稳定 ID 绑定、统一观战目标、独立异常组件、饥饿倒计时、队友耐久条、熟食颜色、真实背包容量和喷气背包燃料显示一起倒退。
- 本地额外体力条只恢复原版布局所有权：`TeammateBarsCoordinator` 不再调用 `ConfigureLocalExtraBar()`，`LocalStaminaBarPatch` 不再调用 `KeepLocalExtraBarVisible()`。PEAK 原版 `StaminaBar.Update()` 继续负责宽度、动画、黑边、闪电图标和石化布局。
- `PI_LocalExtraStaminaValue` 恢复到 `extraBarStamina` 内部，只显示当前整数，例如 `40`；不显示 `+40/100` 或 `40/100`。
- 灵魂/观战状态继续通过 `DisplayCharacterHelper.GetObservedOrLocal()` 使用 `observedCharacter -> localCharacter`，本轮没有回退观战其他人的功能。
- 队友克隆出来的 `ExtraStaminaBar` 由 `RemoveClonedExtraBarVisuals` 从克隆树内移除，只保留队友安全侧 `current/cap` 数值。所有删除都验证 `IsChildOf(cloneRoot)`，不会再碰本机原版 HUD。
- `TeammateInventoryRow` 的紧凑喷气背包燃料条改动明确保留。本地额外体力条和队友物品栏燃料条是两个不同功能，不得互相回退。
- `Display.OffsetY` 所有锚点的默认值恢复为 `0`，删除底部锚点从 `0` 自动迁移到 `138` 的逻辑；额外条布局不再通过整体上移 HUD 解决，已有自定义偏移继续保留。

## 证据与状态

- 历史提交 `3bd84c8` 曾把 `PI_LocalExtraStaminaValue` 挂在 `bar.extraBarStamina`；历史 `FixBarGroupOnce()` 不调用 `ConfigureLocalExtraBar()`。
- PEAK 反编译的 `StaminaBar.Update()` 自己维护额外条缩放、序列动画、图标、外框和石化位置，因此 PlayersInfo 不应重复接管这些尺寸。
- Release 构建命令 `dotnet build PlayersInfo.csproj -c Release --no-restore` 已通过，结果 `0` warnings / `0` errors。
- profile DLL：`C:\Users\Administrator\AppData\Roaming\r2modmanPlus-local\PEAK\profiles\2.0.a\BepInEx\plugins\PlayersInfo.dll`，版本 `0.2.1.0`，大小 `83968`，时间 `2026/8/17 22:35:35`，SHA-256 `7732C6A23AB4C1AC9493A9BB25EA8B9AF14AE292DF5AAEE11E231C170896A2CF`；Release 构建 `0` warnings / `0` errors。
- 本地额外条实机确认后，临时 `[LocalExtraRuntime]` 采样和未调用的旧接管方法已移除，恢复默认安静日志。

## 下一步

- 启用新 DLL 并重新启动游戏，验证飞机餐、格兰诺拉、熟食和其他额外体力物品能正常生效和消耗；旧 DLL 已经销毁的运行时 UI 不能靠热替换恢复。
- 验证队友异常状态条仍与各自主体力条对齐，并确认本地异常条没有缺失。
- 实机确认图一式原版缩进、短宽度、黑边、闪电图标和石化段均正确，绿色填充内部只显示当前值。
- 切换灵魂/观战目标，确认额外体力值跟随被观察玩家。
- 确认队友额外图形条没有出现，同时队友侧数值正常。
- 确认物品栏紧凑喷气背包燃料条未受影响。
