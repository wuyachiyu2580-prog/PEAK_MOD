# DreamyAscent Temporary Memory - 2026-05-19

## Temporary Rule Init

- This file stores compressed task conclusions for DreamyAscent after every 3 clear judgments.
- After context compaction or AI handoff, read the newest file in this folder before `RECENT.md`, `DECISIONS.md`, and root `TODO.md`.
- Store conclusions, evidence, risk, and next step only. Do not store raw hidden reasoning.

## 2026-05-19 Official Segment Generation Log Review

- Judgment 1: Beach official `Generate Segment` no longer shows the old zeroing failure. Latest log ends with `Generated segment: Beach_Segment, groupers=4`; segment delta reports `missingGroupers=0->0`, `missingSteps=0->0`, and every grouper delta has `zeroedSteps=<none>`.
- Judgment 2: Current source no longer matches the older memory sentence that Late groupers use a replacement late pipeline. `DaRuntimeEditService.RunGrouperGeneration()` now calls `grouper.RunAll(true)` for all groupers, then runs `RunLateStepsAfterOfficialRunIfNeeded()` only when no external `PropGrouper.RunAll` postfix is detected. Log phrase `manual late-step supplement used` is therefore expected for Late groupers.
- Judgment 3: Runtime item cleanup and regeneration are active. Before `WallProps`, the log destroyed spatially matched berries, mushrooms, and coconuts, then assigned 27 PhotonView IDs after regeneration; final `WallProps` runtime summary shows nearby runtime items present again. DreamyAscent errors were not seen in this slice; only generation trace warnings and WhySoLaggy flood alerts appeared.
- Risk: Because the current implementation uses `RunAll(true)` plus late supplement, formal memory from 2026-05-18 should be corrected if this behavior is accepted as the current direction.
- Next step: If the in-game visual result looks correct, update `RECENT.md`, `DECISIONS.md`, `CHANGELOG.md`, and relevant TODO entries to replace the outdated "Late replacement pipeline" note with the current `RunAll(true) + guarded late supplement` model.

## 2026-05-19 Runtime Items And Beach Color Follow-up

## 2026-05-19 Beach Material And Placement Scale Fix

## 2026-05-19 Revert Material Replay And Fix Jungle Late

- Judgment 1: User requested reverting the last material/scale version because Beach terrain material still appeared default. Reverted the post-generation material modifier replay and custom placement child-scale synchronization; the current build keeps only the earlier official-segment fixes that made Beach coconuts/items normal.
- Judgment 2: Latest Jungle log explains item disappearance: `Jungle_Segment` generated only the two Late prop groupers, but both reported `lateSupplementSteps=0`; after `PropGrouper.RunAll(true)`, many Late PropSpawner steps had `children -> 0`, and postrefresh found `itemSpawners=0`.
- Judgment 3: Root cause is the guard in `RunLateStepsAfterOfficialRunIfNeeded()`: it skipped DreamyAscent's manual late-step supplement when an external `PropGrouper.RunAll` postfix was detected, but the external postfix did not repopulate Jungle Late steps in this official segment path. Fix: always run DreamyAscent's manual late-step supplement after `RunAll(true)`.
- Risk: Always supplementing Late steps may duplicate work only if an external postfix also executes Late steps correctly. Current logs prove the guard is unsafe for Jungle; verify next test shows `manual late-step supplement used` and `lateSupplementSteps>0` for Jungle `Pops_Plat`/`Props_Wall`.
- Next step: Test Jungle `Generate Segment` again. Expected: no large `children -> 0` collapse on prop steps, postrefresh should find item spawners, and Jungle runtime items should remain/spawn.

- Judgment 1: User confirmed Beach coconuts/items are now normal after the Early-before-Late and segment-level runtime refresh fixes. Latest logs also show `official segment grouper order` as `PlateauRocks(Early) -> WallRocks(Early) -> PlateauProps(Late) -> WallProps(Late)`.
- Judgment 2: Beach material still appears like default Beach. Logs show `variant=BlueBeach` and `Using current variant default template... variant=BlueBeach`; ObjectReferenceMap confirms BlueBeach material modifier references such as `M_Basalt_Blue`, `M_Foliage Generic_Blue`, and `M_SaltRock`. `frozenRandomMaterials=0` was misleading because many random-material arrays are single-material or generated objects had no visible candidate material before rerun.
- Judgment 3: Added post-generation material modifier replay for official generation. After `RunAll(true)` and late supplement, DreamyAscent now reapplies material modifiers (`PSM_SetMaterial`, `PSM_SetMaterialOnChild`, `PSM_SetMaterialsOnChild`, `PSM_SetRandomMaterial`, and replace-material modifiers where applicable) to generated children under each PropSpawner, with `replayedMaterialModifiers` logged.
- Judgment 4: Added placement scale propagation for custom placement runtime objects. After a placed parent receives its random/local scale, all descendants recursively get the same localScale so child objects visually match the parent scale instead of staying at prefab-local size.
- Risk: Material replay may still not affect non-PropSpawner-generated static terrain renderers if their color is controlled outside PropSpawner modifiers. Next logs should be checked for nonzero `replayedMaterialModifiers`; if it is nonzero and the visual is still default Beach, inspect `Renderer.sharedMaterials` after generation for BlueBeach rock/grass children.
- Next step: Test BlueBeach `Generate Segment` again and inspect the new `replayedMaterialModifiers` log. Test custom placement parent scaling on an object with children and confirm descendants match the parent scale.

- Judgment 1: Runtime pickup/rope generation is currently done per grouper after each grouper is regenerated. Because the sequence is cleanup -> RunAll -> refresh for each grouper, a later grouper cleanup can delete runtime items that an earlier grouper just spawned. Latest log shows `WallProps` deleting many `Item_Coconut(Clone)` entries within the 18m coconut cleanup radius.
- Judgment 2: The "items only appear in one area" symptom is consistent with that sequence. Terrain groupers all regenerate, but only Props groupers have runtime item spawners; Rocks groupers report `itemSpawners=0`. A segment-level two-phase flow is needed: cleanup all runtime spawns for all included groupers, run all terrain groupers, then refresh runtime spawns for all groupers.
- Judgment 3: The Beach color issue is not currently explained by wrong variant selection. The log says `RunSegment enter... variant=BlueBeach`; template snapshots loaded; `Using current variant default template... variant=BlueBeach, groupers=4/4`; `TemplateBaselineReport` is ready with warnings 0. RuntimeExport and baseline paths are all under `Map/Biome_1/Beach/Beach_Segment/BlueBeach`.
- Judgment 4: Color mismatch is more likely from material modifier behavior than variant mis-selection. BlueBeach contains many `PSM_SetMaterial`, `PSM_SetMaterialOnChild`, and `PSM_SetRandomMaterial` steps. Decompiled `PSM_SetRandomMaterial` chooses `mats[Random.Range(...)]` and writes `renderer.sharedMaterial`; set-material modifiers also write `sharedMaterial`.
- Risk: Re-running official generation can legitimately re-randomize materials inside the same BlueBeach material pools. If users expect "same current visual theme, only reroll positions", DreamyAscent must snapshot material choices or restore renderer materials after generation. If the color is truly from another Beach variant, add diagnostics that log selected material names per material modifier before/after generation.
- Next step: Fix runtime pickup generation with segment-level cleanup/refresh first. For color, add trace around material modifiers or snapshot generated renderer material names by grouper/step to distinguish expected random material from cross-variant/shared-material contamination.

## 2026-05-19 Official Segment Generation Fix

## 2026-05-19 Rebuild After Revert And Jungle Item Fix

- Judgment 1: User requested returning to the previous version because Beach terrain material still appeared default. Current source has no post-generation material modifier replay and no placement child-scale propagation; `DaPlacementRuntimeService` only applies `instance.transform.localScale = prefab.transform.localScale * Mathf.Max(0.01f, scale)`.
- Judgment 2: The latest Jungle failure log was produced by an older DLL: it still printed `replayedMaterialModifiers=0`, a field no longer present in current source. That log also showed the known Late-skip failure: `Pops_Plat`/`Props_Wall` had `lateSupplementSteps=0`, many PropSpawner children collapsed to 0, and postrefresh found `itemSpawners=0`.
- Judgment 3: Decompiled `PropGrouper.RunAll(true)` clears all LevelGenSteps but only executes Early steps directly; Late execution is hidden behind the compiler-generated `Done` local function and is not reached by the public method path alone. Therefore DreamyAscent must always run its manual Late supplement after `RunAll(true)` for official segment generation. Removed the now-unused external Harmony postfix guard to avoid skipping this again.
- Build: `dotnet build .\MOD开发\DreamyAscent\DreamyAscent\DreamyAscent.csproj -c Release` passed with 0 warnings and 0 errors, outputting `C:\Users\Administrator\AppData\Roaming\r2modmanPlus-local\PEAK\profiles\terrain\BepInEx\plugins\DreamyAscent.dll`.
- Next step: Restart/reload the game so the new DLL is loaded, then click Jungle `Generate Segment`. Expected log: no `replayedMaterialModifiers`, `manual late-step supplement used` for Late groupers, `lateSupplementSteps>0`, and postrefresh should find nonzero item spawners/items.

## 2026-05-19 Official Segment Generation Fix Round 2

- Judgment 1: Latest log proved round-1 segment-level runtime cleanup/refresh executed: `segment runtime spawns preclean complete`, then postrefresh reported `PlateauProps itemSpawnedViews=46` and `WallProps itemSpawnedViews=74`. The remaining item symptom is therefore not simply "later grouper cleanup deleted earlier items."
- Judgment 2: Decompiled `PropGrouper.RunAll()` shows official grouping semantics rely on Early steps before Late steps. DreamyAscent was running groupers in UI/list order (`PlateauProps` Late before `PlateauRocks` Early), so props/items could spawn against old terrain/colliders before rocks/terrain changed. Fix: sort official segment groupers by `PropGrouperTiming` so all Early groupers run before Late groupers, then do the existing segment-level runtime refresh.
- Judgment 3: Decompiled `PropSpawnerMod` shows `PSM_SetRandomMaterial` is a serializable modifier object inside `PropSpawner.modifiers`, not a Unity `Component`. Round-1 `GetComponentsInChildren<Component>()` scan could only log `frozenRandomMaterials=0`. Fix: scan each `PropSpawner.modifiers`, freeze `PSM_SetRandomMaterial.mats` to the currently visible candidate-material subset, then restore original arrays after generation.
- Risk: In-game verification is still required. Expected log after the new DLL: grouper order should show Early groupers (`PlateauRocks`, `WallRocks`) before Late groupers (`PlateauProps`, `WallProps`), and `frozenRandomMaterials` should be nonzero if BlueBeach random material modifiers have candidate materials currently visible under their spawner children.
- Next step: Test `Generate Segment` again on BlueBeach. If items still visually cluster, inspect whether preview/camera export hides Plateau vs Wall items despite logs showing both groups spawn; if colors still drift with nonzero freezes, snapshot modifier material names before/after generation.

- Judgment 1: The runtime pickup "only one area" symptom was fixed in `DaRuntimeEditService.RunOfficialSegment()` by changing official segment generation to a two-phase flow: collect all eligible groupers, pre-clean runtime items/ropes for the whole segment once, regenerate all groupers with per-grouper cleanup/refresh disabled, then refresh runtime spawns for all generated groupers once at the end.
- Judgment 2: The Beach color drift was addressed conservatively by freezing `PSM_SetRandomMaterial.mats` during `PropGrouper.RunAll(true)`. The freeze only happens when the current renderer material is found in the candidate pool; otherwise the modifier is skipped, so the fix avoids forcing a first-candidate material and making colors worse.
- Judgment 3: Release build passed with `dotnet build .\MOD开发\DreamyAscent\DreamyAscent\DreamyAscent.csproj -c Release`, producing `C:\Users\Administrator\AppData\Roaming\r2modmanPlus-local\PEAK\profiles\terrain\BepInEx\plugins\DreamyAscent.dll` with 0 warnings and 0 errors.
- Risk: Runtime behavior still needs an in-game click test because the material freeze depends on existing renderer materials being visible under the owning `LevelGenStep` before regeneration. Logs should now show segment-level preclean/postrefresh and `frozenRandomMaterials`.
- Next step: In game, click official template `生成本段` on BlueBeach and confirm pickups appear across the whole segment and Beach colors stay consistent with the current variant.
