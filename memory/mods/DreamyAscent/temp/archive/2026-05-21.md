# DreamyAscent Temporary Memory - 2026-05-21

## 2026-05-21 End-of-Day Closeout

- Today ends with DreamyAscent focused on official `Generate Segment` stability. The latest code change is the zero-output PropSpawner backup/restore safety net in `DaRuntimeEditService.RunOfficialSegmentRootLevelGeneration()`.
- Current diagnosis: latest logs did not show `levelgen-*-failed`, `TargetInvocationException wrapper`, or other caught generation exceptions. Jungle/Roots/Snow empty props are treated as normal-return zero output: `PropSpawner.Execute()` clears old children first, then raycast/material/constraint/spawn rules produce no replacements.
- Implemented safety behavior: before selected root pipeline execution, selected `PropSpawner`, `PropSpawner_Line`, and `PropSpawner_Sphere` direct children are moved into hidden `DreamyAscent_GenerationBackup`. After execution, only a step that had children before and now has direct child count `0` restores its old children. Steps that generated anything discard their backup. If generation aborts, all backups restore.
- Added diagnostics for the next retest: `zero-output PropSpawner diagnostics` logs active state, transform, area/ray/layer/spawn params, props, constraints/modifiers/post constraints/post behaviors, plus deterministic ray samples with hit path/layer/tag/material.
- Build/deploy state: Release build passed with 0 warnings / 0 errors. Deployed DLL is `C:\Users\Administrator\AppData\Roaming\r2modmanPlus-local\PEAK\profiles\terrain\BepInEx\plugins\DreamyAscent.dll`, size `421376`, timestamp `2026/5/21 0:24:28`.
- Next session start here after compaction: retest Jungle/Roots/Snow official `Generate Segment`. Expected log fields are `official segment root level pipeline used`, `official segment root pipeline used`, `backedUpSteps=<n>`, and, if the old zero-output failure still happens, `restoredZeroedSteps>0` plus `zero-output PropSpawner diagnostics`. Visually, the segment should not be wiped empty.
- Beach status: coconuts/items are normal. Beach terrain material is still unresolved and still appears like default Beach. Do not restore the failed post-generation material modifier replay or child-scale sync experiments; later material work needs fresh renderer/material diagnostics.
- Keep current decisions: do not restore external Harmony postfix guard logic; do not use `Go()` as the Late supplement; do not re-enable whole Jungle/Roots rock groupers just to recover props. Use selected-step root pipeline plus diagnostics.

## 2026-05-21 Generate Segment Variety Diagnosis

- User retest log path: `C:\Users\Administrator\Desktop\MOD\PEAK\主客机日志`. Result: official `Generate Segment` works again, but repeated generation looks too uniform.
- Log evidence: each click uses `Using current variant default template for segment generation`, e.g. Beach `variant=RedBeach`, Roots `variant=Default`, Desert `variant=TornadoHell`, Caldera/Volcano blank variant. This means DA is rerunning selected steps inside the current active variant/template, not rerolling biome variant or rebuilding the whole map.
- Code evidence: `DaRuntimeEditService.RunOfficialSegmentRootLevelGeneration()` collects selected `LevelGenStep`s and executes Early/Late timing, but does not call `UnityEngine.Random.InitState`, does not save/restore `UnityEngine.Random.state`, and does not choose a new map seed per click. Original full map generation seeds `Random` during `MapHandler.Start`; DA's manual per-segment path currently inherits current runtime random state only.
- Variation is also limited by official data: many spawners have fixed counts / `randomSpawns=false` and tight constraints. Volcano only ran 10 selected steps, all Early, and changed mostly `Start`/`Middle` line spawners; Caldera changed mainly `LavaRivers`.
- Roots-specific clue: repeated Roots click logged `restoredZeroedSteps=24`; many zero-output diagnostics show inactive hierarchy and ray samples hitting `FogSphereSystem/FogOrigins/Beach/Fogwall_Next` or no hit. The zero-output backup is preventing wipes, but it also preserves old children for failed steps, so later clicks visibly change only the steps that successfully regenerate.
- Safe next fix if user wants more variety: add a per-click segment generation seed around the official root-level step execution. Save `UnityEngine.Random.state`, call `UnityEngine.Random.InitState(seed)`, run selected Early/Late steps, then restore state before runtime item spawning; log `generationSeed=<seed>`. This should improve truly random spawners but will not fully change deterministic/fixed line spawners or switch biome variants.
- Larger variety requires a higher-risk mode: reroll/activate variant or run a fuller root `RunAll(true)` / TerrainRandomiser-style rebuild for the segment. Avoid doing that until beach material/default-variant issue and zero-output raycast context are understood.

## 2026-05-21 Generate Segment Seed Reroll Patch

- Implemented the safe variety fix in `DaRuntimeEditService.RunOfficialSegmentRootLevelGeneration()`: before selected official segment steps execute, DA now creates a per-click `generationSeed`, saves `UnityEngine.Random.state`, calls `UnityEngine.Random.InitState(generationSeed)`, then restores the previous random state in `finally`.
- Log expectation: `Generation trace: official segment root level pipeline used` now includes `generationSeed=<int>`. Repeated clicks should show different seeds.
- Scope: this improves variation for step logic that consumes Unity random during the manual segment run. It intentionally does not reroll active biome variant/template and does not re-enable risky whole-rock-group regeneration.
- Build/deploy: `dotnet build MOD开发\DreamyAscent\DreamyAscent\DreamyAscent.csproj -c Release` passed with 0 warnings / 0 errors and emitted `C:\Users\Administrator\AppData\Roaming\r2modmanPlus-local\PEAK\profiles\terrain\BepInEx\plugins\DreamyAscent.dll`, size `421888`.

## 2026-05-21 Generate Segment Fast Diagnostics Patch

- User asked to speed up `Generate Segment` and noted terrain can still look partly similar. Checked `C:\Users\Administrator\Desktop\MOD\PEAK\主客机日志`; latest log file timestamp was still `2026/5/21 20:12` and contained no `generationSeed=`, so it is pre-seed-patch or game-not-restarted evidence. It still shows why terrain can look similar: current-variant templates are reused and many zero-output steps restore old children.
- Implemented fast default diagnostics in `Plugin.cs` and `DaRuntimeEditService.cs`: new BepInEx config `Generation.DetailedGenerationDiagnostics` defaults to `false`. Startup logs `Generation diagnostics mode: fast` or `detailed`.
- Fast mode keeps safety and core logs but skips heavy before/after segment/grouper snapshots, grouper/segment delta logging, zero-output raycast diagnostics, per-item cleanup logs, per-item refresh logs, post-generation nearby item/rope scans, and backup descendant counting. Detailed mode restores the verbose diagnostics when bug hunting.
- `Generated segment` now logs `elapsedMs=<n>`. `official segment root level pipeline used` still logs `generationSeed=<int>`, `restoredZeroedSteps`, selected/early/late counts, and frozen random material count.
- Build/deploy: `dotnet build MOD开发\DreamyAscent\DreamyAscent\DreamyAscent.csproj -c Release` passed with 0 warnings / 0 errors and emitted `C:\Users\Administrator\AppData\Roaming\r2modmanPlus-local\PEAK\profiles\terrain\BepInEx\plugins\DreamyAscent.dll`, size `423424`.
- Next retest: restart game, click `Generate Segment` twice on the same segment, then check new `LogOutput.log` for different `generationSeed` values and `elapsedMs`. If terrain still looks too similar despite different seeds, next fix is not logging/perf but a higher-risk variant/template reroll or fuller segment rebuild mode.

## 2026-05-21 Retest Log Review After Fast Patch

- Current live log is not under desktop `主客机日志`; it is `C:\Users\Administrator\AppData\Roaming\r2modmanPlus-local\PEAK\profiles\terrain\BepInEx\LogOutput.log`. Desktop log folder had no `LogOutput.log` during this review.
- Fast diagnostics loaded correctly: log has `Generation diagnostics mode: fast`. New per-click seeds are present and differ: Beach `1552716334`; Roots `1280256331`, `-1475060546`, `1807489591`; Desert `-1133820012`; Caldera `-1137809278`; Volcano `198536946`.
- Elapsed times from log: Beach `56471ms` (still very slow), Roots about `8065-8173ms`, Desert `7913ms`, Caldera `3910ms`, Volcano `2382ms`. Beach slowness is now not mainly verbose diagnostics; log shows Beach cleaned 124 runtime items, assigned 40 PhotonView ids, and refreshed 390 item spawners producing 132 item views.
- Template diagnostics are healthy but fixed-template: latest diagnostics `S_20260521_221644_10deb7cd` has `TemplateSnapshotMatchReport` matched 5/5, baseline ready 5, warnings 0. Runtime variants remain fixed: Beach `RedBeach`, Roots normalized `Default` with active `- Redwoods Default Variant`, Desert `TornadoHell`, Caldera/Volcano no variant.
- Generated children snapshot summary: segments=5, steps=165, generatedChildren=9545, looseObjects=28, specialObjects=136, relationshipCandidates=698, dirty=False; external map modifiers and warnings are empty.
- Important Roots clue: skipped-grouper whitelisted rock steps still log `step delta` and several become `children -> 0`: `Rings`, `Small Rocks`, `Pillars`, `Caves`, `Shelf Shrooms`, `Big Redwood`. This suggests the old whitelist path can still clear failed rock/structure steps without the zero-output backup safety. Next fix should either disable these whitelisted risky rock steps, or run them through backup/restore like the selected root pipeline.

## 2026-05-21 Whitelisted Step Zero-Output Guard

- Implemented the safer path for skipped high-risk grouper whitelisted steps in `DaRuntimeEditService.RunWhitelistedStepsForSkippedOfficialGrouper()`.
- Old behavior: `ClearDirectGeneratedChildren()` ran before `TryInvokeLevelGenGo()`. If the whitelisted rock/structure step generated zero children, Roots could lose `Rings`, `Small Rocks`, `Pillars`, `Caves`, `Shelf Shrooms`, `Big Redwood`, etc.
- New behavior: DA backs up a whitelisted step's direct children with `BackupSingleStepChildren()` before invoking `Go`. If `Go` fails, the backup is restored. If `Go` succeeds but leaves direct child count at 0, `RestoreZeroedStepBackupAndDiscardOthers()` restores the old children. If generation succeeds with children, the backup is discarded.
- Log expectation: `Generated whitelisted step from skipped official grouper` now includes `restoredZeroedSteps=<0|1>`. In fast mode, detailed zero-output raycast info remains suppressed unless `Generation.DetailedGenerationDiagnostics=true`.
- Build/deploy: Release build passed with 0 warnings / 0 errors and emitted `C:\Users\Administrator\AppData\Roaming\r2modmanPlus-local\PEAK\profiles\terrain\BepInEx\plugins\DreamyAscent.dll`, size `423936`.
