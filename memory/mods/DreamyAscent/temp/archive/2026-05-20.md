# DreamyAscent Temporary Memory - 2026-05-20

## 2026-05-20 Zero-Output PropSpawner Backup Recovery

- Judgment 1: Latest `LogOutput.log` after full stack logging still had no `levelgen-*-failed`, no `TargetInvocationException wrapper`, and no caught exception stack. The failure path is normal-return zero output: selected root pipeline executes the intended steps, then many PropSpawner steps report `children -> 0`.
- Judgment 2: Because `PropSpawner.Execute()` begins with `Clear()`, a failed raycast/material/constraint pass destroys the old children before proving it can spawn replacements. Patched `DaRuntimeEditService.RunOfficialSegmentRootLevelGeneration()` to move direct children of selected `PropSpawner` / `PropSpawner_Line` / `PropSpawner_Sphere` steps into a hidden `DreamyAscent_GenerationBackup` before execution.
- Judgment 3: After official generation, DreamyAscent now restores the old children only when a backed-up step had children before and has direct child count `0` after execution. If a step successfully generated any direct child, its backup is discarded. If generation aborts before success cleanup, all backups are restored. This is a safety guard against empty Jungle/Roots/Snow, not a final fix for why constraints/raycast/materials reject a spawn.
- Added diagnostics: restored zero-output PropSpawner logs `zero-output PropSpawner diagnostics` with active state, transform, `area`, ray params, `layerType`/mask, spawn counts/chance, props, constraints/modifiers/post constraints/post behaviors, and 5 deterministic ray samples with hit path/layer/tag/material.
- Build: temporary verify build passed 0 warnings / 0 errors, then normal Release build also passed and deployed to `C:\Users\Administrator\AppData\Roaming\r2modmanPlus-local\PEAK\profiles\terrain\BepInEx\plugins\DreamyAscent.dll`. Deployed DLL size `421376`, file timestamp `2026/5/21 0:24:28` on this machine.
- Next retest: click official `Generate Segment` for Jungle/Roots/Snow. Expected log now includes `backedUpSteps=<n>` and, if the old failure still happens, `restoredZeroedSteps>0` plus `restored previous generated children after zero-output official generation`; visually, props should no longer be wiped to empty. If visual is still wrong, inspect the new ray sample/material diagnostics before changing generation semantics again.

## 2026-05-20 Jungle Official Segment Fix

- Judgment 1: User retested Jungle after removing the external postfix guard; Jungle was still empty. Latest log proved the new DLL was loaded because `replayedMaterialModifiers` disappeared, but `Pops_Plat` and `Props_Wall` still logged `lateSupplementSteps=0` and many PropSpawner steps collapsed to `children -> 0`.
- Judgment 2: The remaining root cause was not the external postfix guard. `CollectLateStepsForGrouper()` used Unity `GetComponentInParent<PropGrouper>()`, which can fail to identify the nearest parent grouper when inactive hierarchy is involved. The Late supplement therefore collected zero steps even though the grouper snapshot showed 9/18 steps.
- Judgment 3: Fixed `CollectLateStepsForGrouper()` to find the nearest parent `PropGrouper` by manually walking `Transform.parent`, including inactive parents. This should make Jungle `Pops_Plat` collect about 9 Late steps and `Props_Wall` about 18 Late steps. Build passed and output DLL was updated at `2026/5/20 0:09:54`.
- Risk: In-game verification is still needed because runtime constraints/raycast may still reject some individual steps. Expected log after reload: `manual late-step supplement used` for Jungle Late groupers, `lateSupplementSteps>0`, no broad `Bushes/Trees/Vines -> 0` collapse, and postrefresh should find runtime item spawners again.
- Current state: Beach coconuts/items are normal; Beach terrain material still unresolved and the failed material replay/child-scale sync changes remain reverted.

## 2026-05-20 Late Supplement Execute Pipeline Fix

- Judgment 1: Latest logs after the inactive-safe parent fix proved Late step collection now works: Roots and Snow logged `manual late-step supplement used` with nonzero `lateSupplementSteps`. However many PropSpawner/DecorSpawner steps still collapsed to `children -> 0`, and postrefresh still found `itemSpawners=0`.
- Judgment 2: The remaining issue is execution semantics, not collection. DreamyAscent was invoking `Go()` for Late supplement, while decompiled `PropGrouper.RunAll` executes Late steps with `LevelGenStep.Execute()` and then runs `DeferredStepTiming.AfterCurrentGroupTiming` deferred behaviors as a group.
- Judgment 3: Fixed `RunLateStepsAfterOfficialRunIfNeeded()` to call `step.Execute()`, collect `IDeferredStep` by `DeferredTiming`, and execute `AfterCurrentGroupTiming` deferred steps after all Late steps. The log now includes `deferredAfterGroup=<n>` on `manual late-step supplement used`.
- Build: Release build passed with 0 warnings and 0 errors. DLL updated at `C:\Users\Administrator\AppData\Roaming\r2modmanPlus-local\PEAK\profiles\terrain\BepInEx\plugins\DreamyAscent.dll`, timestamp `2026/5/20 20:57:50`.
- Next step: Retest Jungle/Roots/Snow official `Generate Segment`. Expected: nonzero `lateSupplementSteps`, likely nonzero `deferredAfterGroup` for spawners with post behaviors, and fewer/no broad prop steps collapsing to 0. If still 0, inspect constraints/raycast state after terrain groupers.

## 2026-05-20 Generation Code/JSON Recheck

- Judgment 1: Runtime is loading the generated Snapshot V2 file, not the stale source data copy. `.csproj` copies `data/map-data/generated/template-snapshots.json` to `BepInEx/plugins/DreamyAscent Data/template-snapshots.json`; deployed file hash/size matches generated (`6804281`, generatedAt `2026-05-17`). The older `DreamyAscent Data/template-snapshots.json` remains stale (`6506629`, generatedAt `2026-05-14`) but is not the build input.
- Judgment 2: The generated JSON contains complete PropSpawner/DecorSpawner coverage for the failing areas. Jungle has `Pops_Plat` and `Props_Wall` with Bushes/Trees/Vines/Thorns/etc.; Roots has `PlateauProps`, `WallProps`, `Redwood`, rocks, and Waterfalls; Snow has PlateauProps/WallProps/Rocks/IceRockSpawn groupers. Missing JSON entries are not the current cause of empty props.
- Judgment 3: The main code divergence is generation granularity. DreamyAscent `RunOfficialSegment()` filters and then runs each child `PropGrouper` individually, while TerrainCustomiser segment generate calls the segment root `PropGrouper.RunAll(true)` and TerrainRandomiser also uses a root-wide pipeline. Logs show nested/related groupers being cleared or reduced when run piecemeal (for example Snow `Rocks` clears nested `IceRockSpawn_L/R`, and Roots/Snow PropSpawner steps collapse to zero). The next likely fix is to replace official segment generation with a root-wide segment pipeline plus the existing late supplement/runtime item refresh, while preserving Beach behavior and avoiding high-risk full Root/Jungle rock reruns until verified.

## 2026-05-20 Official Segment Root-Scoped Pipeline Fix

- Judgment 1: A raw segment-root `RunAll(true)` is too broad for DreamyAscent because Roots/Jungle intentionally skip high-risk rocks (`PlateauRocks`/`WallRocks`, `Rocks_Plat`/`Rocks_Wall`). The fix therefore uses a root-scoped but selected-grouper pipeline: build the allowed grouper list from current variant baseline and skip policy, then execute only LevelGenSteps whose ancestor `PropGrouper` is in that list.
- Judgment 2: The new official segment path keeps the official order semantics without piecemeal child-grouper `RunAll` side effects. It clears only selected steps, executes selected Early steps, flushes `AfterCurrentGroupTiming` deferred behavior, then executes selected Late steps with the same inactive-safe nearest-parent grouper filter. Runtime item/rope cleanup, PhotonView ID assignment, and runtime item/rope postrefresh remain segment-level.
- Judgment 3: Release build passed with 0 warnings and 0 errors. Output DLL: `C:\Users\Administrator\AppData\Roaming\r2modmanPlus-local\PEAK\profiles\terrain\BepInEx\plugins\DreamyAscent.dll`, timestamp `2026/5/20 21:39:17`, size `409600`. Expected retest logs: `official segment root level pipeline used`, `official segment root pipeline used`, `earlySteps>0` for terrain/rock groups, `lateSupplementSteps>0` for prop groups, and far fewer/no `Bushes/Trees/Vines/Pine/Shrub -> 0` collapses.
- Risk: This is still a runtime pipeline approximation of original root generation with selected-grouper filtering. If a skipped rock grouper owns a dependency required by props, retest logs should show which prop steps still zero out; then prefer a step-level whitelist over restoring whole high-risk rock groupers.

## 2026-05-20 Exception Stack Logging Patch

- Judgment 1: Latest `LogOutput.log` proved the root-scoped pipeline is loaded, but the remaining failure is still visible as Snow/Roots prop groups going to `children -> 0` and postrefresh reporting `itemSpawners=0`. The log did not contain a useful caught exception stack yet.
- Judgment 2: `DaRuntimeEditService` still had several generation/runtime catch blocks logging only `ex.Message`, and some `DaLog.OnceWarn` keys were based only on step type. That can hide the real failing instance when many steps share the same `PropSpawner` type.
- Judgment 3: Patched generation-related catches to log full `Exception.ToString()`. `TargetInvocationException` is now expanded to show the inner exception stack first plus the reflection wrapper. LevelGen clear/execute/go and grouper validate warnings now include hierarchy path in both key and message, so same-type steps no longer mask each other.
- Build: Release build passed with 0 warnings and 0 errors. Output DLL: `C:\Users\Administrator\AppData\Roaming\r2modmanPlus-local\PEAK\profiles\terrain\BepInEx\plugins\DreamyAscent.dll`, size `410112`.
- Next log triage: search for `levelgen-execute-failed`, `levelgen-clear-failed`, `levelgen-deferred-failed`, `runtime-item-spawn-refresh-failed`, `runtime-rope-refresh-failed`, `grouper-validate-afterwards-failed`, and `TargetInvocationException wrapper`.

## 2026-05-20 Root Pipeline Selected-Step Narrowing

- Judgment 1: The refreshed log still has no `levelgen-*-failed`, `TargetInvocationException wrapper`, or other DreamyAscent caught exception. This means the empty props are not an exception path; `LevelGenStep.Execute()` returns normally while PropSpawner/constraints/raycast produce zero children.
- Judgment 2: The old root pipeline over-selected runtime steps by scanning the whole segment root and filtering by ancestor grouper. In Roots it logged `activeSteps=267` while the allowed official groupers only had 32 exported steps. That can mix nested/variant/extra runtime steps into the run and leaves too much behavior dependent on current scene hierarchy state.
- Judgment 3: Patched the root official pipeline to execute only the exported selected steps from `groupersToGenerate`, split by their owning `PropGrouper.timing`: selected Early steps -> `AfterCurrentGroupTiming` deferred -> selected Late steps -> deferred. Added `Physics.SyncTransforms()` after clear and between Early/Late so late PropSpawner raycasts see the rebuilt colliders.
- Build: code compiled successfully with 0 warnings and 0 errors to temporary verification output `C:\Users\Administrator\Desktop\MOD\PEAK\.appdata\build-verify\DreamyAscent\DreamyAscent.dll`. Normal Release deployment to r2modman failed only because `PEAK.exe` is still running and locking `BepInEx\plugins\DreamyAscent.dll`.
- Next deployment: after closing PEAK, rerun `dotnet build ".\MOD开发\DreamyAscent\DreamyAscent\DreamyAscent.csproj" -c Release` so the fixed DLL overwrites the profile plugin. Expected new log field: `selectedSteps=<n>` instead of `activeSteps=<n>`, plus `lateSteps=<n>` and `deferredAfterLate=<n>`.
