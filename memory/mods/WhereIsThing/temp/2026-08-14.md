# WhereIsThing 阶段恢复记忆

本轮已完成可编译基础版。关键事实：2.1.a 物品数据库公开 `itemLookup`；物品名称字段是 `UIData.itemName`，游戏名称索引是 `NAME_` 前缀；背包内容位于 `Item.data` 的 `BackpackData.itemSlots`，槽位物品由 `ItemSlot.prefab` 表示；`ItemState` 仍包含 Ground、Held、InBackpack。

实现边界：扫描按选择组内全部 itemID 匹配，地面/手持显示真实 Item Transform，落地背包内容显示背包 Transform，未打开行李箱走 `Luggage.ALL_LUGGAGE`；窗口默认 Alt+C，扫描默认 C，语言支持 Game/English/简体中文，显示支持常驻/计时，范围支持地面/手持/背包/行李箱。类别是 UI 筛选启发式，不能当成游戏官方分类；窗口分类内使用自适应多列网格。

验证结论：Release 构建 0 warnings / 0 errors，尚未实机。下一步优先验证 ItemDatabase 加载时机、窗口输入穿透、地面/手持/背包标签、语言字体和大量目标性能。
